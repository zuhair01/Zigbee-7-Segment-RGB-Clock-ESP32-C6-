## Managed component dependencies pulled from the ESP Component Registry.
## `idf.py reconfigure` (or the first build) downloads these automatically.
dependencies:
  espressif/esp-zboss-lib: "~1.6.0"
  espressif/esp-zigbee-lib: "~1.6.0"
  espressif/led_strip: "^3.0.0"
  idf:
    version: ">=5.3.0"
